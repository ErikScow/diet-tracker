import React from 'react';

import Nav from '../common/Nav'

function Historical(props) {
    return (
        <div>
            <Nav />
        </div>
    );
}

export default Historical;